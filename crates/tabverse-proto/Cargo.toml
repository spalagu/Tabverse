[package]
name = "tabverse-proto"
version = "0.1.0"
edition = "2021"
description = "Shared message types between the Tabverse app, remote hosts and clients."
license.workspace = true
homepage.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
